#include "main.h"
/**
 * mul - multiplication of 2 integers
 * @a: int to be multiplied
 * @b: int to be multiplied
 * Return: multiplication of a and b (success)
 */
int mul(int a, int b)
{
	return (a * b);
}
